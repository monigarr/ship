# Epic 7 — Agent-as-citizen

**Before:** FleetGraph used internal DB paths only.  
**Fix:** `SHIP_AGENT_USE_PUBLIC_API=true` seeds first-party OAuth app; probe calls SDK `/me`, `documents.list`, `issues.list`; `fetchContextDocuments` reads via SDK instead of direct SQL.  
**After:** Agent path uses the same public API contract as external integrations; audit middleware logs `client_id`.  
**Proof:** `agent-platform.test.ts`; grep `platform_audit_log` for `client_id` after agent run with flag on.
