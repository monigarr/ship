# Epic 7 — Agent-as-citizen

**Before:** FleetGraph used internal DB paths only.  
**Fix:** `SHIP_AGENT_USE_PUBLIC_API=true` runs SDK `/me` probe via seeded first-party OAuth app; audit middleware logs `client_id`.  
**After:** Agent path proves public API contract before internal shortcuts.  
**Proof:** `agent-platform.test.ts`; grep `platform_audit_log` for `client_id` after agent run.
