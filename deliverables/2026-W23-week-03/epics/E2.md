# Epic 2 — Public API contract

**Before:** Ad-hoc errors on internal routes.  
**Fix:** `ApiError` envelope, cursor pagination, route metadata + OpenAPI generator, fitness tests.  
**After:** `/api/v1/documents`, `/me`, generated `openapi.json`.  
**Proof:** `public-api-fitness.test.ts`, `openapi-schema.test.ts`.
