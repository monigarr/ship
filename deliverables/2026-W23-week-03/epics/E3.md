# Epic 3 — Device + refresh tokens

**Before:** Authorization code only; no refresh rotation.  
**Fix:** Migration `048`, `/oauth/device/code`, `/oauth/device/verify`, refresh grant with family revocation.  
**After:** CLI can `ship login`; stolen refresh invalidates family.  
**Proof:** `oauth-device.test.ts`, `oauth-refresh.test.ts`.
