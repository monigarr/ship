# Epic 6 — CLI + TTFE

**Before:** No reference integration consuming `@ship/sdk`.  
**Fix:** `integrations/cli` (`ship login`, `docs`, `webhooks tail`), `pnpm drill:ttfe`.  
**After:** Five-line developer story runnable against deploy.  
**Proof:** `integrations/cli/tests/ttfe.drill.ts`; CI `platform-gates.yml` + `scripts/platform/run-ttfe-ci.mjs`.
