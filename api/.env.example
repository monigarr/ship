# Ship API - Local Development Configuration
# Copy this to .env.local and customize values:
#   cp .env.example .env.local

# Database (matches docker-compose.yml defaults)
DATABASE_URL=postgresql://ship:ship_dev_password@localhost:5432/ship_dev

# Session secret (change in production)
SESSION_SECRET=dev-only-secret-do-not-use-in-production

# CORS origin (must match frontend URL)
CORS_ORIGIN=http://localhost:5173

# File uploads (required in production for direct-to-S3 uploads)
# S3_UPLOADS_BUCKET=ship-uploads-dev-123456789012

# Environment
NODE_ENV=development

# Server port (optional, defaults to 3000)
PORT=3000

# ====================
# FleetGraph / Observability
# ====================
# Proactive mode (scheduled poll + Ship mutation webhooks). Set to 1 in production.
# FLEETGRAPH_PROACTIVE_ENABLED=1
# FLEETGRAPH_POLL_INTERVAL_MS=180000
# FLEETGRAPH_PLANNING_POLL_INTERVAL_MS=300000
# FLEETGRAPH_HEALTH_POLL_INTERVAL_MS=21600000
# FLEETGRAPH_COMPLIANCE_POLL_INTERVAL_MS=86400000
# FLEETGRAPH_WEBHOOK_DEBOUNCE_MS=30000
# Optional LLM synthesis for on-demand chat when detectors find signals (requires AWS Bedrock)
# FLEETGRAPH_SYNTHESIS_ENABLED=1
# Optional base path for internal trace links shown in the UI
# FLEETGRAPH_TRACE_BASE_PATH=/fleetgraph/traces
# Optional LangSmith dual-emission (additive; internal Ship traces remain canonical)
# LANGSMITH_API_KEY=lsv2_...
# LANGSMITH_PROJECT=ship-fleetgraph
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LANGSMITH_PUBLIC_BASE_URL=https://smith.langchain.com
# LANGSMITH_PROJECT_TRACES_URL=https://smith.langchain.com/o/53ea29ad-725a-449d-8454-a5e5b940ea6c/projects/p/94ee9aa8-6f2b-42b6-80d5-d5c18af5729d?runview=traces
# Optional per-run URL template; {runId} will be replaced with FleetGraph traceId
# LANGSMITH_RUN_URL_TEMPLATE=https://smith.langchain.com/o/53ea29ad-725a-449d-8454-a5e5b940ea6c/projects/p/94ee9aa8-6f2b-42b6-80d5-d5c18af5729d/r/{runId}

# ====================
# PIV Authentication (Optional)
# ====================
# Configure one or both providers. If neither is configured, only password auth is available.

# FPKI Validator - Primary PIV authentication provider
# Credentials are loaded from AWS Secrets Manager at startup (preferred)
# or from environment variables (fallback)
# FPKI_ISSUER_URL=https://fpki-validator.example.gov
# FPKI_CLIENT_ID=your-client-id
# FPKI_CLIENT_SECRET=your-client-secret
# FPKI_REDIRECT_URI=http://localhost:3000/api/auth/piv/callback
# FPKI_SECRET_NAME=ship/fpki-oauth-credentials

# CAIA (Treasury Customer Authentication & Identity Architecture)
# Alternative PIV authentication provider via Treasury's CAIA
# Note: CAIA's `sub` is NOT persistent - email is the primary identifier
# CAIA_ISSUER_URL=https://caia-dev.treasury.gov
# CAIA_CLIENT_ID=your-client-id
# CAIA_CLIENT_SECRET=your-client-secret
# CAIA_REDIRECT_URI=http://localhost:3000/api/auth/caia/callback
# CAIA_SECRET_NAME=ship/caia-credentials
