{
  "name": "@ship/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Ship platform CLI reference integration",
  "type": "module",
  "bin": {
    "ship": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:ttfe": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@ship/sdk": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
